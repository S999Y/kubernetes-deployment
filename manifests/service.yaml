apiVersion: v1
kind: Service
metadata:
  name: node-express-app
  namespace: production
  labels:
    app: node-express-app
spec:
  type: NodePort
  selector:
    app: node-express-app
  ports:
    - name: http
      port: 80
      targetPort: 4000
      nodePort: 30080